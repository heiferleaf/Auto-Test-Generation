# CodeBuddy 集成测试报告

## 预期结果契约（来自 fixtures/codebuddy-expected.md）
# CodeBuddy 集成测试 - 预期结果说明

> 本文件由 `test/integration-codebuddy.test.ts` 读取，作为"操作预期结果"的人类可读契约。
> 测试运行时对照实际输出，生成 `test/reports/codebuddy-run-<timestamp>.md` 报告（预期 vs 实际）。

## 步骤 1：连接 CodeBuddy（端口 9222）
- 预期：连接成功，无 CDP 错误（CdpError）
- 预期：listTargets 至少返回 1 个 page 类型目标 + 若干 webview（编辑器/侧栏/终端/AI 面板）
- 预期：主目标（page）可被选为操作目标

## 步骤 2：快照主窗口可交互元素
- 预期：snapshot 返回非空节点列表
- 预期：能找到至少一个输入框（role=textbox）或按钮（role=button）

## 步骤 3：打开命令面板（Ctrl+Shift+P）
- 预期：出现命令输入框（role=textbox 或特定 locator 可见）
- 预期：eval('document.activeElement.role') 或快照中出现输入框

## 步骤 4：截图主窗口（M2 可视化能力，实现前 skip）
- 预期：截图非空白（尺寸 > 0，文件存在）
- 说明：依赖 M2 新增的 `adapter.screenshot`，实现前本条以 skip 存在

## 步骤 5：视觉断言 - 侧栏可见且在视口内（M2，实现前 skip）
- 预期：locateVisual(侧栏) 返回 visible=true 且 rect 在视口范围
- 说明：依赖 M2 新增的 `adapter.locateVisual` 与 `elementVisibleInViewport` 断言 kind

## 步骤 6：文本断言 - 标题/欢迎包含预期文案
- 预期：textContains 某已知文案（如应用名 "CodeBuddy"）通过
- 说明：验证断言引擎在真机可用

## 失败处理
- 任一步失败：测试输出失败步骤 id 与快照，报告标红，不静默通过。


## 实际结果
- listTargets 返回 4 个目标
  - page: 1, webview: 3
- 主窗口 snapshot 节点数: 152
- document.title = "发行说明: 4.11.1 - CodeBuddy CN [管理员]"
