# `.codebuddy/` 归档

本目录是 CodeBuddy 时期的配置归档，**Cursor 不会加载这里的 skill / agent**。

现行配置：

- `AGENTS.md`
- `.cursor/rules/`
- `.cursor/skills/`

新的 git worktree 放到 `.cursor/worktree/`，不要再往 `.codebuddy/worktree/` 加。
