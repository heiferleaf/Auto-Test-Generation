# 可视化蒙版（M3）功能与交互规格

> **状态：待用户确认后冻结。确认前不得改 `src/` / `test/` 产品逻辑。**
>
> 来源：确认稿《可视化蒙版_功能点与用户交互逻辑_确认稿_删除版本管理_CFG统一展示》+ 用户补充。
> **冲突以补充为准。** 本期删除/降级 Git 式版本管理；步骤用 **CFG 图形化控制流图统一展示**（节点+有向边，步骤多时折叠/缩放/minimap）。
>
> 配套：`docs/architecture/architecture.md`、`docs/plan/plan.md`、Figma 原型 `ATG M3 可视化面板原型`、`docs/design/m3-visual-panel-prototype.html`。
> 本文档是 M3 UI 的强制交互基线。实现后产品符合度须按 §2.x 逐条验收；UI 主链路走 `test/ui-core-e2e.test.ts` 的 `app.boot()` + `[data-action]`，禁止只用内部 API 直调冒充用户路径。

---

## 0 核心范式

可视化蒙版是 **CFG 步骤工作台 + 靶机内真实操作捕获层**，不是独立脚本播放器，也不是在网页截图上模拟点靶机。

两条面必须分开：

| 面 | 用户能做什么 | 用户不能做什么 |
|---|---|---|
| **工作台**（本地网页） | 看连接/录制/运行状态；看/编 CFG；插入等待与断言；组织组；运行全部 | **不能**在网页里点出靶机里的真实交互 |
| **靶机**（真实软件） | 开始录制后在软件里点击/输入；点选录制时在软件里点目标元素 | 不能用工作台点击代替这些操作 |

核心链路：

```text
连接靶机
  → 开始录制
  → 用户在靶机内真实操作（点击/输入/选择/提交）
  → 交互实时生成 Step，CFG 实时增长
  → 停止录制
  → 查看 / 编辑 / 删除 / 补充等待与断言
  → 框选或选组，组织为 Sequence / Selection / Loop
  → 运行全部
  → 执行中 CFG 高亮当前步；失败立即暂停并定位
```

**步骤正确性**：按步骤执行，就是用户想在软件里执行的内容。录制必须能完整定位多层 DOM，输入必须坍缩成一次填充。插入的等待/断言必须能设时间和目标。

**编排正确性**：一步默认就是一个组；多选可打包、命名、拆包；选中组可设为选择组或循环组。CFG 以**图形化控制流图**呈现这些结构——顺序串成链、选择分两枝、循环画回环，步骤多时仍可读（折叠/缩放/minimap）。

**交互友好性**：拖拽调序；左键按住框选后弹出「分支组 / 循环组」；等待元素、断言、创建分支组的条件，都要能进入「嵌入式点选录制」，让用户在真实软件里点元素。

---

## 1 功能边界

| 模块 | 包含 | 不包含（本期） |
|---|---|---|
| 连接 | 自动连默认可配置端口；失败提示重连；未连禁用录制/运行 | 不在网页里模拟靶机点击 |
| 录制 | 开始/停止；靶机内 click/input/change/submit 实时成步 | 不提供手动添加 click/fill/select/submit |
| 点选录制 | 等待元素、断言、选择组条件：进入点选态后在靶机点元素 | 不在截图流上点选冒充靶机点选 |
| 插入 | wait（时长）、waitUntil（元素+超时）、assert（元素或文本） | 不插入 snapshot/note/callScript/setVar |
| 组 | 默认一步一组；打包/命名/拆包；设为顺序/选择/循环 | 不自动依赖检测、不级联删除 |
| 展示 | **CFG 图形化控制流图**统一展示控制流与步骤；步骤多时可折叠/缩放/minimap | 不把线性列表当主视图；不挂 Git 版本面板 |
| 运行 | 运行全部；失败立即暂停+标红+截图定位；可选从此处运行 | 不静默跳过失败步 |
| 版本 | — | **Git 式 commit/branch/switch/cherry-pick 不进入本期 UI** |

---

## 2 元素、功能与交互逻辑

### 2.0 工作台布局（信息架构）

本期工作台仍是本地网页（浏览器跑不了 Playwright，真机必须走 Node 桥）。靶机内常驻叠加条/浮动钮作为增强项，见 §5 决策 D1。

```text
┌──────────────────────────────────────────────┐
│ 顶部条：连接状态 / 录制状态 / 运行状态 / 重连   │
├──────────────────────────────────────────────┤
│ CFG 主视图（步骤 + Sequence/Selection/Loop）  │  详情/编辑
│ 拖拽调序 · 框选打包 · 选组设 kind               │  类型 / 定位(封装)
│                                              │  参数 / 保存删除
│ 截图流：查看与运行高亮（辅助面，不是操作面）     │  「在软件中点选」
├──────────────────────────────────────────────┤
│ 开始/停止录制 · 运行全部 · 插入步骤 · 错误提示   │
└──────────────────────────────────────────────┘
```

| 元素 | 功能 | 交互逻辑 |
|---|---|---|
| 顶部条 | 显示连接/录制/运行 | 已连接 / 未连接 / 已暂停 / ● 录制中；录制与运行变化时实时更新 |
| 录制钮 | 开始/停止录制 | 未连接时禁用。点开始后**必须**到靶机里操作，工作台截图上的点击不产生步骤 |
| 运行全部 | 从头执行 | 未连接时禁用。失败立即暂停，见 §2.8 |
| 插入步骤 | 只增控制类步 | 菜单仅：等待时间 / 等待条件 / 断言。循环与分支走组操作，见 §2.4、§2.5 |
| CFG 画布 | 统一展示全部步骤与控制流（图形化） | 主视图。节点+有向边：顺序链/选择分叉/循环回环。点击节点→选中+详情+截图高亮。拖拽改顺序。框选打包。组可折叠、画布可缩放平移、minimap 定位 |
| 详情区 | 编辑选中步/组 | 不同类型不同字段；定位对人显示封装文案，底层仍存完整 locator |
| 截图流 | 查看与高亮辅助 | 不作为点靶机的操作面 |

### 2.1 连接

| 元素 | 功能 | 交互逻辑 |
|---|---|---|
| 连接指示 | 显示是否连上靶机 | 启动后自动连默认可配置调试端口（默认 9222，须能被环境变量/`?cdp=` 覆盖，禁止写死到唯一路径且不可改） |
| 失败态 | 提示并允许重连 | 未连接 → 提示失败原因 + 重连入口；开始录制与运行全部不可用 |
| 目标选择 | 切主窗口/WebView | 本期可维持自动选主目标；选择器不能写死应用名。P3 再做完整手动目标 UI |

### 2.2 录制：用户操作步的唯一入口

用户操作类步骤（点击 / 填充 / 选择 / 提交）**只能**由「开始录制」在靶机里捕获，工作台不提供手动添加。

| 元素 | 功能 | 交互逻辑 |
|---|---|---|
| 开始录制 | 注入监听 | 向当前及后续出现的 target 注入 click/input/change/submit；顶部进入 ● 录制中 |
| 实时生成 | 边操作边出步 | 每次有效操作立即变成 Step 并在 CFG 追加节点，**不是**停录后批量生成 |
| 录制高亮 | 刚操作的元素 | 靶机或截图流上出现录制色高亮框 |
| 停止录制 | 结束捕获 | 停止监听；已生成步骤保留，进入可编辑 |

#### 2.2.1 点击：多层 DOM 完整定位 + 封装展示

靶机里点到的往往是多层嵌套节点（例如 `button > span > icon`）。只记最内层 `role/name` 会在回放时点偏或点不到。

**捕获（存进 Step.locator，回放用）**

1. 被点元素自身的语义属性：`role` / `name`（aria-label、name、可见文本）/ `testId`
2. **祖先链**：从被点节点走到可交互祖先（button/a/input/[role] 等）的路径，至少包含 css 路径或 xpath，保证能唯一定位
3. `target`（哪个 window/webview）

**展示（给人看，不把整串 DOM 甩脸上）**

- 封装成一句人话：`点击 [确定]`、`点击 [搜索框]`
- 详情区默认证「封装名」；需要时再展开完整路径（css/xpath/祖先）

#### 2.2.2 输入：只保留一次填充，不要中间按键

用户输入「你好」时，IME 与逐键会打出 `n` / `ni` / `ni h` / … / `你好` 一串 input。这些**全部是冗余**。

**规则**

- 同一输入框的连续 input/change **坍缩为一条 fill**，值为最终文本
- 结束坍缩的时机：焦点离开、按 Enter、用户点了别的元素、停止录制
- 不能只在「当前 250ms 缓冲」里合并——轮询 drain 跨窗口仍必须合并，否则慢打字会拆成多步
- 展示：`填充 [搜索框] = 你好`

### 2.3 嵌入式点选录制（补充强制，旧实现没有）

等待元素、断言、创建选择组的条件，都依赖「在真实软件里指定一个元素」。工作台表单填 `role=status` 占位**不算完成**。

这不是第二次「开始录制整段流程」，而是嵌在上述步骤里的 **点选子模式**：

```text
用户在工作台点「在软件中点选」
  → 系统进入点选态（顶部提示「请在靶机中点击目标元素」）
  → 用户切到真实软件，点一个元素
  → 系统用与 §2.2.1 相同的完整 locator 写回当前步骤/组条件
  → 退出点选态，工作台详情区显示封装名
```

| 用在哪 | 点选写入什么 |
|---|---|
| 等待条件 waitUntil | 等待目标元素的 locator + 用户填的最长等待时间 |
| 断言「元素可见/存在」 | 断言目标 locator |
| 断言「存在文本」 | 可选点选所在元素；目标文本由用户填写 |
| 选择组的执行条件 | 与断言同一套：元素 + 可选期望文本 |

约束：

- 点选必须发生在靶机，不能点工作台截图冒充
- 未连靶机时「在软件中点选」禁用并提示先连接
- 点选过程中不要把这次点击写进普通录制步骤列表（它是在给当前表单填 locator）

### 2.4 手动插入步骤

用户操作类步骤不能手动创建。插入入口只提供 3 类（补充优先：循环不是插入项，是组的 kind，见 §2.5）。

入口：CFG「+」/「插入步骤」

```text
+ 添加步骤
├── 等待时间
├── 等待条件
└── 断言
```

#### 等待时间 `wait`

| 元素 | 功能 | 交互逻辑 |
|---|---|---|
| 时长输入 | 设置等待毫秒 | 用户填写（如 3000），保存后 CFG 节点显示「等待 3000ms」 |

用途：等模型回答、等页面加载。不需要点选元素。

#### 等待条件 `waitUntil`

| 元素 | 功能 | 交互逻辑 |
|---|---|---|
| 在软件中点选 | 指定等待哪个元素出现/可见 | 走 §2.3 点选录制 |
| 最长等待 | 超时毫秒 | 用户填写（如 10000），超时仍不满足则该步失败（§2.8） |

生成：`等待 [封装名] 出现，最长 10000ms`。

执行语义：在超时内轮询目标断言，成立即继续；**禁止**只 sleep `timeoutMs` 假装等到了。

#### 断言 `assert`

| 元素 | 功能 | 交互逻辑 |
|---|---|---|
| 断言类型 | 可见 / 存在 / 文本 等 | 用户选 kind |
| 在软件中点选 | 元素类断言的目标 | 走 §2.3；「元素可见」「元素存在」必须能选元素 |
| 目标文本 | 文本类断言 | 「存在文本 / 文案包含」可填期望字符串 |

断言既用于验证，也可作为选择组的判断条件（与 §2.5 同一套目标模型）。

### 2.5 组：默认一步一组，再打包 / 设分支 / 设循环

补充对编排的要求（覆盖确认稿里单独「插入循环」和「用 assert 搭 if」的旧说法）：

- 连续执行的步骤是 **组**
- **一个步骤默认就是一个单独的组**
- 多选一些步骤可 **打包为组**，组可 **命名**、可 **拆包**
- **选中组** 再设为选择组或循环组
- 选择组的执行条件，和断言的判断目标可以是同一套（点选元素 / 设文本）
- 循环组可设循环次数

| 元素 | 功能 | 交互逻辑 |
|---|---|---|
| 默认组 | 每条新步骤（录制或插入） | 自身即一个顺序组（组内 1 步）。用户不用先「建组」才能选中它 |
| 框选 / 多选 | 选出连续或若干步骤/组 | **鼠标左键按住拖框**选中；也可点选配合修饰键。松手后弹出：设为分支组 / 设为循环组 / 仅打包为顺序组 |
| 打包 | 合成一个命名顺序组 | 弹出后可选「仅打包」；组名可改（默认如「组 3」） |
| 拆包 | 解开组 | 选中组 → 拆包：子步骤/子组回到原层级，不删内容 |
| 设为选择组 | if / else | 选中组 → 设为选择。须配置执行条件（§2.3 点选 + 可选文本）。**当前组内步骤进入 True 分支；False 初始为空**，可再拖入或框选填入（§5 决策 D4） |
| 设为循环组 | while ×N | 选中组 → 设为循环，填写次数。组内步骤为循环体 |
| 组名 | 识别 | 详情区可改名；CFG 节点显示组名而非内部实现 id |

数据层继续用现有 `Step.children` + `Step.control.kind = sequence | if | while`，不另起第三套 schema（§5 决策 D6）。UI 把每个节点都当「组」操作：叶子 = 只有一步的顺序组。

**选择组与执行器必须一致**：`children[0]` = True，`children[1]` = Else。画反则用户看见的分支和真跑的相反。打包成选择组时，不得把 N 条叶子直接塞进 `children` 当「第 1 条 then、第 2 条 else、其余丢弃」。

### 2.6 CFG 统一展示（图形化控制流图）

确认稿文件名要求 **CFG 统一展示**。本期 **CFG 是步骤的唯一主视图**，且必须以**图形化控制流图**呈现：节点表示步骤/组，有向边表示执行流向。不再并排维护一套可编辑线性列表当主操作面（§5 决策 D2、D11）。

> 关键诉求（用户确认 Figma 原型后补充）：**步骤多的时候，CFG 要能形象地以图示表现步骤运行的结构**——分支往哪分、循环往哪回、顺序怎么串，一眼可读，而不是一列扁平编号。

**图示语义（与执行器一致，画反即谬误）**

```text
Sequence（顺序）        Selection（选择 / if-else）      Loop（循环 ×N）
   ┌──────┐               cond ──┬── True → …            ┌──<──┐
   │ Step │                      │                        │     │
   └──┬───┘                      └── False → …           ▼     │
      ↓                                                 body → ┘
   ┌──────┐
   │ Step │
   └──────┘
```

- **顺序**：竖向链，节点间自上而下的顺序边。
- **选择**：条件节点分叉出 `True` / `False` 两条分支边；`children[0]=True`、`children[1]=Else`（与执行器 `runNode` 一致，§2.5）。
- **循环**：循环体执行后画**回环边**回到循环头，并标注 `×N`；回环边回到循环头而非末步。

基础结构：

```text
Sequence
│
├── Step（默认即一组）
├── Step
│
├── Selection（条件 = 断言同类目标）
│   ├── True → Sequence
│   └── False → Sequence
│
└── Loop × N
    └── Sequence
```

| 元素 | 功能 | 交互逻辑 |
|---|---|---|
| 控制流图 | 展示组嵌套与流向 | 顺序竖排+顺序边；选择组分 True/False 分支边；循环画回环边并标 ×N |
| 选中 | 查看/编辑 | 点节点 → 详情区 + 截图高亮该元素（若有 locator） |
| 拖拽调序 | 改执行顺序 | 在 CFG 上拖节点调整兄弟顺序；不得拖进会破坏 if/while 合法性的位置（例如把 True 拖成与条件平级且无提示） |
| 框选 | 打包并选 kind | 见 §2.5 |
| 运行态 | 跟随当前步 | 执行到某节点则该节点高亮；失败变红（图上与列表同步） |
| 删除 | 只删当前 | 选中 → 删除。不级联删后续；依赖坏了由运行时暴露 |

#### 2.6.1 CFG 图示渲染规则

CFG 是**渲染层**，不改数据模型：仍消费 `Step.children` + `control.kind`（§5 决策 D6），图模型与 DOM 渲染分离（`buildCfgGraph(script)` 纯函数产图模型，`CfgView` 只画图与上报点击，不 import 内核/执行器——DIP）。

**节点形态**

| 节点 | 形态 | 标注 |
|---|---|---|
| 叶子步（点击/填充/选择/提交） | 单框 | 封装文案如「点击 [搜索框]」「填充 [搜索框] = 你好」 |
| 顺序组 | 容器框，子节点竖排 | 组名（默认「组 N」）；可折叠为单个节点 |
| 选择组 | 条件框 + 两枝 | 条件文案；True/False 两枝标注 |
| 循环组 | 容器框 + 回环边 | 组名 + `×N`；循环体在框内 |

**边语义**

- 顺序边：父→子，竖向。
- 分支边：条件节点 → `True` / `False`，分别标注。
- 回环边：循环体末 → 循环头，带 `×N` 标注；不得画成顺序边。

**运行态在图上**

- 当前执行步：高亮（与步骤列表同一状态源，§2.8）。
- pass：通过态着色。
- fail：节点标红 + 截图定位失败元素（§2.8）；图上与列表同步标红，禁止只红一处。
- 重跑：图上状态随重跑重置。

**规模可读性（步骤多时仍可读，本期必须覆盖）**

- **折叠**：打包组可折叠为一个节点（仅显示组名 + kind + 计数），展开再看内部；叶子步默认展开。
- **缩放 / 平移**：画布支持缩放与平移，步骤多时整体结构仍可一览。
- **minimap / 滚动定位**：步骤很多时提供小地图或滚动定位，选中节点自动滚入视口。
- **运行跟随**：执行时当前步自动滚入并高亮，用户不必手动找。

**约束**

- 图示必须与执行器真实控制流一致；`children[0]=True` / `children[1]=Else` 画反属最危险缺陷（用户看见的分支和真跑的相反）。
- 未知 `control.kind` 不得白屏：渲染层降级为「未知控制结构」占位节点，硬失败只留在数据入口（`io.ts` / 桥边界）。
- 不把 css/xpath 长串画进节点；节点只显示封装文案，完整路径在详情区展开。

### 2.7 步骤 / 组详情

选中后详情区按类型展示。定位对人显示封装名，完整路径可展开。

| 类型 | 可编辑项 |
|---|---|
| 点击 / 填充 | 封装定位（只读或展开完整路径）；填充还有最终文本 |
| 等待时间 | `waitMs` |
| 等待条件 | 点选目标、`timeoutMs` |
| 断言 | kind、点选目标、期望文本 |
| 顺序组 | 组名、拆包 |
| 选择组 | 组名、条件（点选+文本）、True/False 内容说明 |
| 循环组 | 组名、循环次数、拆包 |

保存 = 不可变更新当前节点。删除 = 只删该节点。可运行性交用户判断，不预检依赖。

### 2.8 运行

| 元素 | 功能 | 交互逻辑 |
|---|---|---|
| 运行全部 | 从第一步执行整棵 CFG | 逐步执行；CFG 与截图流同步高亮当前步 |
| 从此处运行 | 可选 | 选中节点 → 从该节点及之后执行 |
| 失败 | 立即停 | **立即暂停** → 错误提醒 → 该步标红 → 截图定位失败位置 → 高亮框变红。禁止静默跳过 |

失败后用户改 locator/参数 → 保存 → 再运行。

### 2.9 截图流与高亮

截图流是 **查看和定位辅助面**，不是操作靶机的主面。

| 场景 | 系统行为 |
|---|---|
| 点选某步 | 切到该步对应页面截图，高亮元素，浮标如「点击 / 搜索按钮」 |
| 运行跟随 | 当前执行步自动高亮，用户不用再点 |
| 失败 | 高亮框变红，标明是哪个元素导致失败 |
| 录制 | 刚捕获的元素录制色高亮 |

---

## 3 端到端场景（验收用）

**A 录制普通流程**：连接 → 开始录制 → 只在靶机点/输入 → CFG 实时出步（点击完整定位、输入一条 fill）→ 停止 → 改参数 → 运行全部 → 逐步高亮。

**B 补等待与断言**：录制完 → 插入等待时间（填毫秒）→ 插入等待条件（点选元素 + 超时）→ 插入断言（点选或填文本）→ 运行全部。

**C 做 if/else**：框选或选组 → 选「分支组」→ 点选条件元素 → True 已有步骤、False 再填 → CFG 图上看到条件分叉两枝 → 运行全部。

**D 做循环**：选组 → 选「循环组」→ 设次数 → CFG 图上看到回环边 + ×N → 运行全部。

**E 失败修复**：运行失败 → 暂停标红定位（图上与列表同步标红）→ 改 locator/参数 → 再跑。

**F 步骤多时的可读性**：录制/插入大量步骤后 → 打包组折叠为单节点、画布缩放平移、minimap 定位 → 运行时当前步自动滚入高亮，结构仍一眼可读。

---

## 4 本期明确不做

- snapshot / note / callScript / setVar
- 自动依赖检测、自动级联删除
- 脱离靶机的纯文本回放
- 在独立面板或截图流上模拟靶机点击来「生成用户操作步」
- **Git 式版本管理 UI**（commit / branch / switch / cherry-pick / tag / 版本图）。已有 `version-store` / `version-panel` **冻结**：默认不挂载，本期不演进。脚本持久化用导入/导出 JSON 即可
- 靶机内常驻叠加条 + 浮动钮：确认稿想要，但补充承认工作台是网页、操作必须在真实软件。是否做叠加层见 §5 D1，**不阻塞**点选录制与 CFG 组编辑

---

## 5 待用户拍板的决策（实施前必须有答案）

建议值是设计侧默认，不是已经开工。

| ID | 问题 | 建议 | 备选 |
|---|---|---|---|
| D1 | 工作台形态 | **网页工作台 + 靶机内录制/点选**。叠加条/浮动钮放到后续 | 必须先做靶机内叠加层才算 M3 |
| D2 | 线性列表 | **去掉可编辑列表，只留 CFG** | CFG 主视图 + 只读列表辅助 |
| D3 | 「+」是否还插循环 | **不插**。循环 = 选组设 kind | 保留第四项「循环」，插入空 while |
| D4 | 顺序组改选择组时步骤去哪 | **进 True，False 为空** | 弹出让用户选进 True 还是 False |
| D5 | 组命名 | **本期要做**（打包时填、详情可改） | 先用自动名，命名下一期 |
| D6 | 一步一组的数据形态 | **沿用 `Step` + `control`/`children`，UI 把叶子当组**，避免再升 schema | 单独 Group 类型，v3 schema |
| D7 | Git 代码 | **UI 永不挂载；store/panel 冻结不删**（等你明确要求再删） | 连文件一起移出主路径 |
| D8 | 点选是否三处共用一套 | **waitUntil / assert / 选择组条件共用同一点选子模式** | 三套各写各的 |
| D9 | 端口 | **默认 9222，环境变量与 `?cdp=` 可改；未连必须禁用录制/运行** | 本期必须做完整目标选择 UI（原 P3） |
| D10 | 无靶机时 DemoKernel | **`?demo=1` 才演示；默认连真机，失败红条且录制/运行不可用** | 去掉 DemoKernel |
| D11 | CFG 展示形态 | **图形化控制流图为主视图**（节点+有向边，顺序/分叉/回环；步骤多时折叠+缩放+minimap）。不并排可编辑线性列表 | 树状文本骨架为主、图示可选 |

---

## 6 与旧规格的冲突（补充优先，已按此改文档）

| 旧说法 | 现在 |
|---|---|
| Git 与 CFG 融合、版本面板、切分支 | **删除出本期产品**；确认稿文件名已写明 |
| 步骤列表 + CFG 双主视图双向联动 | **CFG 图形化控制流图统一展示**（节点+有向边，步骤多时折叠/缩放/minimap） |
| 「+」四类含 repeat | 插入三类；循环走组 |
| 插入循环后再往体内拖步 | 先有组（默认一步一组或打包），再设循环次数 |
| 用单独 assert 步当 Selection 条件 | 选中组设为选择组，条件用与断言相同的点选/文本 |
| 蒙版必须先做成靶机内叠加层 | 补充：网页无法操作靶机，录制和点选在真实软件里；叠加层不阻塞本期 |
| 等待条件只填「某断言成立」文本 | 必须能点选页面元素 + 最长等待 |
| 断言只填 kind/target 字符串 | 元素类必须点选；文本类必须能设目标文本 |

旧 `visual-mask-ui-spec.md` 的 §4/§6 Git 章节作废。架构里 Git 融合模型改为「已实现但产品降级」，见 `architecture.md` §2.2。
